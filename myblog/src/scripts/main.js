// This file contains the JavaScript code for the blog. 
// It may include functionality for dynamic content, event handling, and any interactive features of the blog.

document.addEventListener('DOMContentLoaded', () => {
    console.log('Welcome to my personal blog!');

    // Example of a simple event listener for a button
    const button = document.getElementById('myButton');
    if (button) {
        button.addEventListener('click', () => {
            alert('Button clicked!');
        });
    }

    // Additional JavaScript functionality can be added here
});